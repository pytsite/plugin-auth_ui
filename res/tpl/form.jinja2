<!DOCTYPE html>
<html lang="{{ current_lang() }}">

<head>
    {% block metatags %} {{ metatags() }} {% endblock %}
    {% block css_tags %} {{ css_tags() }} {% endblock %}
    {% block js_head_tags %} {{ js_head_tags() }} {% endblock %}
</head>

<body id="auth-ui-page" class="auth-ui-page {{ form_type }} driver-{{ driver }}">

{% block messages %}
    <div id="messages" class="container">
        {% for type in ['error', 'warning', 'info', 'success'] %}
            {% set messages = session_messages(type) %}
            {% if type == 'error' %}{% set type = 'danger' %}{% endif %}
            {% if messages|length %}
                <div class="row">
                    <div class="col col-xs-12">
                        {% for msg in messages %}
                            <div class="alert alert-dismissible alert-{{ type }}" role="alert">
                                {{ msg }}
                                <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                                    <span aria-hidden="true">&times;</span>
                                </button>
                            </div>
                        {% endfor %}
                    </div>
                </div>
            {% endif %}
        {% endfor %}
    </div>
{% endblock %}

{% block content %}
    <div id="page" class="container">
        <div class="row">
            <div class="col col-xs-12">
                {% block form %}
                    {{ form }}
                {% endblock %}
            </div>
        </div>
    </div>
{% endblock %}

{% block js_tags %} {{ js_tags() }} {% endblock %}
{% block inline_js %} {{ inline_js() }} {% endblock %}

</body>
</html>
